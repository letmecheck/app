<div class="new-game container">
  <h1>A Form for New Game</h1>
  <%= simple_form_for @game do |f| %>
    <%= f.input :name %>
    <br>
    <%= f.button :submit, class: 'btn btn-primary' %>
  <% end %>
</div>
